# Managed by Ansible for tjuclaw-identity
DSN=postgres://kratos:{{ identity_db_password }}@db:5432/kratos?sslmode=disable
SERVE_PUBLIC_BASE_URL={{ identity_app_url | regex_replace('/+$', '') }}/api/kratos/
SELFSERVICE_DEFAULT_BROWSER_RETURN_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/complete
SELFSERVICE_ALLOWED_RETURN_URLS={{ identity_app_url | regex_replace('/+$', '') }}/auth/complete
SELFSERVICE_FLOWS_LOGIN_UI_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/login
SELFSERVICE_FLOWS_REGISTRATION_UI_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/registration
SELFSERVICE_FLOWS_VERIFICATION_UI_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/verification
SELFSERVICE_FLOWS_ERROR_UI_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/error
SELFSERVICE_FLOWS_LOGOUT_AFTER_DEFAULT_BROWSER_RETURN_URL={{ identity_app_url | regex_replace('/+$', '') }}/auth/logged-out
SECRETS_COOKIE={{ identity_secret_cookie }}
SECRETS_CIPHER={{ identity_secret_cipher }}
