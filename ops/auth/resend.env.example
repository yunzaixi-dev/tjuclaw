# Inject into Kratos/courier, NEVER Vite, a frontend env file, or Git.
# Sender domain must be verified in Resend. URL-encode the API key if necessary.
COURIER_SMTP_CONNECTION_URI=smtps://resend:REPLACE_WITH_RESEND_API_KEY@smtp.resend.com:465/
COURIER_SMTP_FROM_ADDRESS=login@agentwego.com
COURIER_SMTP_FROM_NAME=TJUClaw
