# Only safe local port defaults; no credentials.
API_PORT=8080
WEB_PORT=8088
TASK_DATA_DIR=/var/lib/tjuclaw/tasks
# Campus integration is disabled until the deployment secret store provides it.
# WPY_APP_TICKET=base64(app-key.app-secret)
# WPY_DOMAIN=weipeiyang.twt.edu.cn
