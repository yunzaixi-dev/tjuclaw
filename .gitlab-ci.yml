# Builds and required tests run on GitHub-hosted Actions runners.
# Keep GitLab as the competition repository; do not queue jobs on retired runners.
# See ops/ci/README.md for source synchronization and commit status integration.
workflow:
  rules:
    - when: never

stages: [check]
ci-moved-to-github:
  stage: check
  script:
    - echo "CI runs on GitHub Actions; see ops/ci/README.md"
